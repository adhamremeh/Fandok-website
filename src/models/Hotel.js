class Hotel {

    constructor(HotelName, Address, Contacts, Reservations) {

        this.HotelName = HotelName;
        this.Address = Address;
        this.Contacts = Contacts;
        this.Reservations = Reservations;

    }
}

export default Hotel;